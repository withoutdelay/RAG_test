apiVersion: v1
kind: Service
metadata:
  name: my-project
  labels:
    app: my-project
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: my-project
