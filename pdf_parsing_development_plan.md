# PDF 数据清洗开发计划

> 目标：基于当前真实工程 PDF 的 review 结论，形成一份面向实施的剩余开发计划，聚焦 `PDF -> 清洗 -> 资产保留 -> 安全入库 -> 审计` 主线。  
> 范围：仅覆盖 RAG 前置的数据质量问题，不覆盖生成质量、Prompt 优化和前端体验。

---

## 1. 当前判断

当前我们正在处理的，确实是 RAG 最关键的部分之一。

对这个项目来说，原文档数据清洗已经是主瓶颈：

- 文本说明段落已经基本可用
- 图、表已经能保住，不再丢失
- 安全入库已经能避免坏数据污染主向量库
- 但复杂公式、上下标、图中参数、超大表格仍然不能高置信度自动使用

当前阶段评分：

- 文本型 RAG 可用度：`7/10`
- 复杂电气工程 PDF 清洗成熟度：`6.2/10`
- 高保真知识真值库可用度：`4/10`

当前主线进度判断：

- MVP 向前推进进度：约 `65%`
- 剩余关键开发量：约 `35%`

---

## 2. 本计划的目标

这份计划不追求“一次性解决复杂 PDF 的全部问题”，而是把剩余 `35%` 拆成可落地的阶段。

本轮目标是把系统推进到以下状态：

1. 文本型和普通图文混排型 PDF 可以稳定进入主检索链路
2. 高风险图表、公式、大表不会污染主向量库
3. 被跳过的高价值内容能保留成一等资产，供后续专项重建
4. 高风险资产可以被单独检索、推荐和引用，供售前工程师人工复用
5. 系统能明确区分“可自动信任”和“只能保留待重建 / 待人工复用”的内容
6. 后续 OCR / 表格重建 / 更强 parser 的引入点被提前留好

非目标：

- 本轮不追求让原理图、波形图、电路图自动高保真理解
- 本轮不追求图片公式 100% 自动恢复
- 本轮不在清洗阶段引入人工处理步骤

---

## 3. 当前剩余问题

基于真实样本和最新 review，当前剩余问题主要有 6 类：

### 3.1 文档类型没有分流

- 还没有区分文本型数字 PDF、图文混排工程 PDF、扫描型 PDF
- 这会导致不同类型文档共用一条解析路径，影响上限

### 3.2 前置噪声仍有泄漏

- 封面、买方/卖方、日期、页眉页脚仍会进入前几个 chunk
- 这会污染召回结果

### 3.3 超大表格虽然被隔离，但还没有重建链路

- 当前已实现 `table asset only`
- 但还没有独立的表格 OCR / 重建 pipeline

### 3.4 公式问题仍然停留在“审计可见、自动不可用”

- 公式候选识别已有
- `pix2tex` 已接入
- 但可信结果仍不足

### 3.5 缺少稳定的真实样本基准

- 目前已经有代表性样本，但还不是固定回归集
- 后续优化如果没有基准，容易陷入“感觉变好了”

### 3.6 高风险资产虽已保留，但还不能服务生成阶段

- 现在的图、表、公式资产已经能保住并定位
- 但生成阶段还没有一条“资产推荐”链路
- 当前系统还不能根据章节上下文，推荐最相关的图、表、公式给售前工程师复用

---

## 4. 新开发计划

按优先级拆成 5 个阶段推进。

### Phase 1：解析入口分流与质量闸门

目标：

- 在解析前先给 PDF 做类型分类
- 在解析后明确给出内容可用等级

范围：

- 文档类型分流：文本型 / 图文混排型 / 扫描型
- 将分类结果写入 `raw_documents.meta`
- 审计报告增加：
  - `document_profile`
  - `ingestion_recommendation`
  - `high_risk_content_flags`

产出：

- PDF 类型分类器
- 解析级风险标识
- 审计报告增强

验收标准：

- 新上传 PDF 都带有 `document_profile`
- 审计报告能明确回答：
  - 是否适合直接入主向量库
  - 是否只应保留为 asset
  - 是否建议走后续 OCR / 重建

优先级：`P0`

### Phase 2：文本清洗与 chunk 质量收口

目标：

- 继续提升可直接进入向量库的文本纯度

范围：

- 封面 / 页眉 / 页脚 / 目录继续清洗
- Heading 与短正文混排块继续规整
- 超大表格之外的中小表格安全切分
- 为 chunk 增加更稳定的：
  - `content_risk_level`
  - `front_matter`
  - `needs_asset_lookup`

产出：

- 更稳的 markdown cleaner
- 更稳的 chunk 切分策略
- 更可控的安全入库规则

验收标准：

- 前 5 个 chunk 内封面噪声显著下降
- 中小表格不再整块超长直灌
- 文本检索质量相较当前版本可稳定提升

优先级：`P0`

### Phase 3：表格专项链路

目标：

- 把“超大表格 / 低置信度表格”从普通文本流程中彻底独立出来

范围：

- 维持当前 `table asset only` 策略
- 为 table asset 增加更明确的生命周期字段：
  - `reconstruction_status`
  - `reconstruction_backend`
  - `reconstruction_confidence`
  - `table_profile`
- 设计并预留独立的表格重建 pipeline
- 不在这一阶段实现完整高保真重建，但把接口和队列位置留好

产出：

- table asset 元数据标准
- 表格重建任务模型草案
- 后续 OCR / 表格结构恢复接口设计

验收标准：

- 所有被主向量库跳过的大表都能在 table asset 层被追踪
- 后续可通过独立 job 对 table asset 发起重建
- 主检索不再被大表噪声拖垮

优先级：`P0`

### Phase 4：Asset Retrieval 与生成阶段引用增强

目标：

- 让高风险图、表、公式虽然不自动进入主知识真值库，但可以在生成阶段被检索、推荐和人工复用

详细设计见：

- [asset_retrieval_phase4_design.md](/Volumes/thunder/code/RAG_test/asset_retrieval_phase4_design.md)

范围：

- 为 `figure/table/formula asset` 建立统一的 `Asset Card`
- `Asset Card` 检索信息至少包含：
  - heading
  - caption
  - 前后文
  - 所属章节
  - 文档类型
  - 风险标签
  - 资产类型
- 建立独立于普通 chunk 的 `asset retrieval index`
- 生成阶段支持：
  - 文本检索
  - 资产检索
  - 推荐资产列表输出
- 推荐结果不直接自动插入正文，只提供：
  - 资产来源
  - 推荐原因
  - 原图 / 原表 / 原公式入口
  - 建议替换位置

产出：

- `Asset Card` 元数据模型
- 基于服务层派生的资产检索索引
- 生成阶段的推荐资产接口
- 售前工程师可复用的资产推荐结果结构

验收标准：

- 在章节生成或重写时，可以返回最相关的图、表、公式候选
- 推荐逻辑不依赖原始脏表全文进入主向量库
- 推荐结果能说明“为什么相关”
- 推荐资产默认是参考材料，而不是自动落稿内容

优先级：`P0`

默认实现策略：

- MVP 先不新增 `asset_cards` 表
- 先基于现有 `figure_assets` / `table assets` 在服务层派生 `Asset Card`
- 待资产推荐价值验证后，再决定是否正式落库

### Phase 5：公式专项与真实样本回归

目标：

- 让公式问题从“只能看见”推进到“能稳定判断是否值得自动使用”

范围：

- 公式区域定位增强
- 审计结果从 `manual_review / uncertain` 扩展到：
  - `trusted_candidate`
  - `review_required_candidate`
  - `rejected_candidate`
- 建立真实样本基准集
- 为不同 PDF 类型固定输出审计结果快照

产出：

- 公式区域定位规则增强
- OCR 候选可信度分级
- 真实工程 PDF 回归基准

验收标准：

- 任何 OCR 输出都不会默认进入主知识链路
- 审计报告能清晰区分可信候选和不可用候选
- 每次改动后能对固定样本自动回归

优先级：`P1`

---

## 5. 实施顺序建议

建议严格按下面顺序推进：

1. `Phase 1`
2. `Phase 2`
3. `Phase 3`
4. `Phase 4`
5. `Phase 5`

原因：

- 没有分流和质量闸门，后面的优化会反复误判问题来源
- 没有文本清洗收口，检索质量会持续被脏块干扰
- 表格需要先从主链路独立，再谈专项重建
- 高风险资产先具备“可推荐、可复用”的业务价值，比先追求自动高保真重建更划算
- 公式 OCR 目前最容易产生“假结果”，必须放在更保守的位置推进

---

## 6. 里程碑评分目标

为了避免“做了很多，但不知道质量到底有没有提升”，每个阶段都绑定评分目标。

### 当前基线

- 文本型 RAG 可用度：`7/10`
- 复杂工程 PDF 清洗成熟度：`6.2/10`
- 高保真知识真值库可用度：`4/10`

### 目标里程碑

#### M1：Phase 1 完成后

- 文本型 RAG 可用度：`7.2/10`
- 复杂工程 PDF 清洗成熟度：`6.6/10`

#### M2：Phase 2 完成后

- 文本型 RAG 可用度：`7.8/10`
- 复杂工程 PDF 清洗成熟度：`7.0/10`

#### M3：Phase 3 完成后

- 主向量库纯度：`8/10`
- 复杂工程 PDF 清洗成熟度：`7.3/10`

#### M4：Phase 4 完成后

- 主向量库纯度：`8.2/10`
- 资产复用辅助价值：`7/10`
- 复杂工程 PDF 清洗成熟度：`7.5/10`

#### M5：Phase 5 完成后

- 复杂工程 PDF 清洗成熟度：`7.8/10`
- 高保真知识真值库可用度：`5.5/10`

说明：

- 本计划不会把复杂工程图纸的自动高保真理解直接做到 `9/10`
- 本轮真正目标是“让系统知道哪些能用，哪些不能乱用”

---

## 7. 暂不做

以下内容先不纳入本轮计划：

- 全量替换 `Docling`
- 大规模引入外部商业 OCR 服务
- 对图纸语义进行自动深度理解
- 把原理图/波形图直接转成可自动问答的真值结构
- 在 MVP 阶段让高风险图、表、公式自动写入最终稿

这些方向不是没价值，而是当前性价比不够高，容易把主线拉偏。

---

## 8. 下一步建议

从当前状态出发，建议马上进入：

1. `Phase 1：解析入口分流与质量闸门`
2. 紧接着推进 `Phase 2：文本清洗与 chunk 质量收口`

原因是这两步对现有系统收益最大，而且不会和已经完成的 `safe ingestion + table asset only` 方向冲突。  
完成这两步后，优先进入 `Phase 4：Asset Retrieval 与生成阶段引用增强`，因为它能最快把“已保留但不可自动使用的高风险资产”转化成可被售前工程师复用的能力。
