# Quickstart

最短路径：

1. 启动后端与前端开发服务。
2. 进入仓库后使用 `super-dev start --idea` 作为面向新需求的入口。
3. 当进入 Super Dev 流程时，第一阶段必须明确显示当前阶段是 `research`。

成功标志：

- 已生成 `output/*-research.md`
- 已冻结三文档并记录确认状态
- 预览通过后进入实现与交付阶段

失败恢复：

- 文档确认未完成时，回到三文档修订
- 预览不通过时，先更新 UI/UX 规范，再重做前端

