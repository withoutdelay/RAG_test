# Host Install Surfaces

## Codex CLI

- 安装：`pip install -U super-dev`
- 便捷安装：`uv tool install super-dev`
- 触发：`super-dev:`
- 审计：`super-dev integrate audit --auto`

## Codex Desktop

- 维持与 CLI 相同的 `super-dev:` 首句契约。
- 在宿主恢复时继续使用 `super-dev run --resume`。
