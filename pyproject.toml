[project]
name = "rag-test-product-driven-solution"
version = "2.3.0"
description = "Governed delivery workspace for the product-driven solution iteration."
requires-python = ">=3.11"

[project.scripts]
super-dev = "super_dev.cli:main"
