# 真实样板接入开发计划（13 份试点版）

> 目标：基于当前新增的 `13` 份真实方案样板，启动一轮“小样本、可验证、可迭代”的 `reuse-first` 试点实施。  
> 范围：聚焦历史方案样板的接入、分层建库、检索重构与评测闭环，不覆盖前端交互大改。  
> 约束：当前样板总量仍偏小，其中 `2` 份为扫描件 PDF，不适合直接与可解析数字 PDF 混入同一主复用链路。

---

## 1. 当前判断

当前项目已经走到一个关键转折点：

- 继续单纯调 Prompt，收益会越来越低
- 继续让系统基于“全局 chunk 检索 + LLM 重新生成”，会反复出现：
  - 技术章节废话多
  - 信息密度下降
  - 图、表、公式虽被保留，但没有稳定进入成稿主链路
  - 目录和章节骨架不够像真实方案

当前最合理的方向，不是“立刻全量灌入更多样板”，而是：

`先把这 13 份样板做成一套试点案例库，再围绕案例库重构 reuse-first 主流程。`

这 `13` 份样板已经足够支撑一轮试点，但还不适合直接被当成“最终生产知识库”。

---

## 2. 样板分层策略

### 2.1 当前样板结构

本轮试点样板按文档类型先拆成两组：

- `11` 份可直接解析的数字 PDF / 图文混排 PDF
- `2` 份扫描件 PDF

### 2.2 基本原则

正式决策如下：

1. `11` 份数字 PDF 进入主试点链路
2. `2` 份扫描件单独进入 `OCR / 资产参考` 路线
3. 扫描件在 MVP 阶段不参与主复用块生成
4. 扫描件允许参与：
   - 文档审计
   - 资产保留
   - 图表/大表/公式资产参考
   - 后续 OCR 专项验证

### 2.3 为什么不能直接混用

如果现在把扫描件和数字 PDF 直接混进同一主复用链路，会带来 3 个问题：

- 扫描件文本噪声会拖低检索质量
- 章节复用更容易误召回
- 难以判断“质量差”究竟来自检索逻辑，还是来自扫描件 OCR 质量

因此，当前阶段必须先分流。

---

## 3. 本轮试点的目标

本轮不是要解决“几百份样板入库后的最终生产问题”，而是要先回答 5 个更关键的问题：

1. `Case-first Retrieval` 是否比当前全局 chunk 检索明显更稳
2. 历史方案的目录结构，能否真正服务候选大纲生成
3. `reuse-first` 是否能显著提升技术章节的信息密度
4. 图、表、公式资产能否在生成阶段变成“可推荐、可引用、可人工复用”的对象
5. 小样本条件下，是否已经能建立一套可持续扩展的数据接入与评测机制

本轮试点成功的标准不是“立刻自动生成终稿”，而是：

`生成结果明显更像历史方案的受控复用，而不是从摘要重新写一篇。`

---

## 4. 新的总体实施路线

本轮试点应把当前主线升级为：

`样板台账 -> 批量解析审计 -> Case Library -> Outline Library -> Block Library -> Asset Library -> Case-first Retrieval -> Reuse-first Generation -> Evaluation`

与此前路线相比，最大的变化是：

- 不再让所有 chunk 直接参与全局检索
- 先找最像的案例
- 再在候选案例范围内找目录、找章节块、找资产

也就是说，后续系统不应再默认走：

`全局检索 -> 摘要增强 -> LLM 生成`

而应改为：

`先找相似案例 -> 再做章节级复用`

---

## 5. 试点数据架构

### 5.1 Case Library

每一份历史方案先作为一个案例对象，而不是直接拆成普通向量块。

Case 级至少维护：

- 项目名称
- 行业
- 产品线
- 关键设备 / 系统
- 项目类型
- 年份
- 文档类型
- 是否扫描件
- 可解析程度
- 资产密度
- 质量标签

作用：

- 先检索最相近案例
- 作为后续目录和复用块的候选池

### 5.2 Outline Library

从历史方案中抽取目录树和章节骨架，形成独立的目录结构库。

作用：

- 基于相似案例生成候选大纲
- 避免大纲阶段继续依赖固定模板
- 支持售前工程师先确认目录，再进入章节生成

### 5.3 Block Library

将正文中的高价值技术段落、设备说明段、控制逻辑段、实施条件段、参数说明段抽成可复用块。

每个块至少带：

- 原文内容
- 所属案例
- 所属章节路径
- section class
- 关键设备标签
- 参数敏感度
- 客户特异性
- 是否高复用

作用：

- 章节生成时不再依赖短摘要
- 直接把最匹配的原始技术块全文交给模型做最小改写

### 5.4 Asset Library

继续保留并增强现有：

- figure assets
- table assets
- formula candidates

作用：

- 生成阶段输出推荐资产
- 不让高风险资产污染主文本向量库
- 为后续人工复用和专项重建提供入口

---

## 6. 分阶段开发计划

### Phase A：样板台账与样板分级

目标：

- 先把 `13` 份样板的基本盘摸清

范围：

- 建立样板台账
- 给每份文档打标签：
  - `digital / mixed / scanned`
  - 行业
  - 产品线
  - 关键设备
  - 方案类型
  - 质量等级
  - 是否适合直接进入主试点链路

产出：

- `sample_manifest`
- 文档级 metadata 基线

验收标准：

- 每份样板都能被唯一标识
- 能明确区分：
  - 主试点样板
  - holdout 样板
  - OCR 专项样板

优先级：`P0`

### Phase B：批量解析与审计

目标：

- 对 13 份样板批量跑解析和审计，得到统一质量画像

范围：

- 11 份数字 PDF：走当前 `docling + cleaner + safe ingestion + audit`
- 2 份扫描件：只跑审计、资产保留和 OCR 风险标记，不进主复用链路

产出：

- 批量审计结果
- 文档可用等级
- 样板分流建议

验收标准：

- 每份样板都能产出审计报告
- 能明确识别：
  - 适合主文本复用
  - 适合资产参考
  - 仅适合 OCR 专项

优先级：`P0`

### Phase C：Outline Library 建设

目标：

- 把历史方案的目录结构真正变成可检索对象

范围：

- 从 11 份数字 PDF 中抽取目录树
- 建立目录结构标准表示
- 给目录树打标签：
  - 行业
  - 产品线
  - 关键设备
  - 章节密度
  - 是否偏设备型 / 系统型 / 实施型

产出：

- 目录树索引
- 候选大纲检索接口

验收标准：

- 给定一个相似需求，能先返回历史相近目录结构
- 大纲阶段不再依赖固定章节模板

优先级：`P0`

### Phase D：Block Library 建设

目标：

- 让章节生成真正建立在“原始技术块复用”之上

范围：

- 从 11 份数字 PDF 中抽取高价值技术块
- 形成可复用块索引
- 对块做最小必要标注：
  - section class
  - key device
  - reuse score
  - customer specificity
  - parameter sensitivity

产出：

- 可复用块索引
- 章节级块检索接口

验收标准：

- 章节生成时不再依赖摘要
- 能基于章节目标选出 `top n` 原始技术块全文

优先级：`P0`

### Phase E：Asset Library 接入生成阶段

目标：

- 让图、表、公式从“保留资产”升级为“生成阶段可推荐资产”

范围：

- 将现有 asset retrieval 正式接入案例级过滤
- 优先在选定案例范围内检索资产
- 扫描件资产默认只做参考，不做主文本依据

产出：

- 资产推荐接口
- 章节级推荐资产结果结构

验收标准：

- 章节生成时能返回相关图、表、公式候选
- `page_furniture` 等低价值资产默认降权
- 结果里能解释推荐原因和来源

优先级：`P0`

### Phase F：Case-first Retrieval

目标：

- 把当前“全局 chunk 检索”升级成“先找案例，再找内容”

范围：

- 先检索最相似案例
- 再在该案例集合内做：
  - outline retrieval
  - block retrieval
  - asset retrieval

产出：

- 案例级候选选择逻辑
- 检索范围收缩机制

验收标准：

- 章节级误召回明显下降
- 复用块更贴近案例语境
- 资产推荐相关性明显提升

优先级：`P0`

### Phase G：试点评测与扩容判定

目标：

- 在 13 份样板规模下建立第一版评测闭环

范围：

- 划分：
  - 主训练/调优样板
  - holdout 样板
  - 扫描件专项样板
- 评测指标至少包括：
  - 大纲贴合度
  - 章节复用度
  - 参数保真
  - 冗词率
  - 客户痕迹残留
  - 资产推荐相关性

产出：

- 试点评测报告
- 是否进入下一轮扩容的决策依据

验收标准：

- 能明确知道当前 `reuse-first` 在 13 份样板上的真实收益
- 能判断是否值得扩到更多样板

优先级：`P0`

---

## 7. 样板使用建议

基于当前样板量，建议不要直接把 13 份都作为“调优输入集”。

推荐划分方式：

- `8-9` 份数字 PDF：主试点样板
- `2-3` 份数字 PDF：holdout 评测样板
- `2` 份扫描件 PDF：OCR / 资产参考专项样板

这样做的目的：

- 避免“所有样板都参与调优，结果没有评测基线”
- 避免扫描件噪声直接拖垮主试点判断

---

## 8. 当前实施优先级

如果从现在开始进入开发，建议顺序固定为：

1. `Phase A`：样板台账与分级
2. `Phase B`：批量解析与审计
3. `Phase C`：Outline Library
4. `Phase D`：Block Library
5. `Phase F`：Case-first Retrieval
6. `Phase E`：Asset Library 接入生成阶段
7. `Phase G`：评测与扩容判定

这里有意把 `Asset Retrieval` 放在 `Case-first` 后面，是因为：

- 如果资产检索仍然全局搜，噪声会很高
- 先确定案例范围，再推荐资产，效果会稳很多

---

## 9. 与现有路线的关系

本计划不是推翻现有工程，而是在现有基础上切换主路线。

与现有文档的关系如下：

- [reuse_first_rag_technical_route.md](/Volumes/thunder/code/RAG_test/reuse_first_rag_technical_route.md)
  - 定义总体主路线与生成理念
- [pdf_parsing_development_plan.md](/Volumes/thunder/code/RAG_test/pdf_parsing_development_plan.md)
  - 定义 PDF 清洗、资产保留和安全入库底座
- 本计划
  - 定义 `13` 份真实样板条件下的试点接入与 reuse-first 落地顺序

---

## 10. 最终判断

基于当前条件，最合理的策略不是：

- 继续大量调 Prompt
- 或者立刻把所有样板直接灌入现有全局检索链路

而是：

`先把 13 份样板做成一套可评估的小型案例库，再围绕案例库重构 reuse-first 主流程。`

这轮试点的核心价值不是“规模”，而是“方向正确并能被验证”。

如果这 `13` 份样板试点有效，再进入更大规模样板接入，成功概率会高得多。
